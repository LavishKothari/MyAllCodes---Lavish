#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;
class Element
{
public:
	ll cost, number;
};
bool cmp(const Element&a,const Element&b)
{
	return a.number<b.number;
}
int main()
{
	ll n,m,k,x,s;
	cin>>n>>m>>k>>x>>s;
	vector<Element>ab(m);
	vector<Element>cd(k);

	for(int i=0;i<m;i++)
		cin>>a[i];
	for(int i=0;i<m;i++)
		cin>>b[i];
	for(int i=0;i<k;i++)
		cin>>c[i];
	for(int i=0;i<k;i++)
		cin>>c[i].cost;
	


	return 0;
}