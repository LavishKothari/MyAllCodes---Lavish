#include<stdio.h>
#include<vector>
#include<algorithm>
using namespace std;
typedef long long int ll;
int main()
{
	ll t;
	char str[5001];
	scanf("%lld",&t);
	while(t--)
	{
		int n,q;
		scanf("%d%d",&n,&q);
		scanf(" %s",str);
		for	(int i=0;i<q;i++)
		{
			int l,r,k;
			scanf("%d%d%d",&l,&r,&k);

		}
		return 0;
	}
