#include<stdio.h>

int main()
{
    int*m,*f;
    int a-1,b-1;
    scanf("%d",&n);

    for(i=0;i<n;i++)
    {
        m=(int*)malloc(sizeof(int)*n);
        f=(int*)malloc(sizeof(int)*n);
        scanf("%d%d",&m[i],&f[i]);


    }

}
