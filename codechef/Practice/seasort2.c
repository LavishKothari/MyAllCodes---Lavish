#include<stdio.h>
int main()
{
    int t,i,n;
    scanf("%d",&t);
    int arr[10000];
    while(t--)
    {
        scanf("%d",&n);
        for(i=0;i<n;i++)
        {
            scanf("%d",&arr[i]);
        }

    }
}
