#include<stdio.h>
int *seive(int n)
{
    int i;
    int*A;
    A=(int*)malloc(sizeof(int)*n);
    for(i=2;i*i<n;i++)
    {
        if(A[j]==0)
        for(j=i*i;j<=n;j++)
        {

        }
    }
}
int main()
{
    int
    return 0;
}
